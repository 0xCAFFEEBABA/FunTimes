using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class LanguageManager2 : MonoBehaviour
{
    /*public TextMeshProUGUI  MainMenu, Play, FamilyCard, SexyCard, MachoCard, GirlyCard, DaringCard, SchoolCard;
    public Language[] language;
    private void Start()
    {
        var index = PlayerPrefs.GetInt("Language");
        CurrentLanguage2(index);
    }

    public void CurrentLanguage2(int index)
    {
        Play.text = language[index].Play;
        FamilyCard.text = language[index].FamilyCard;
        SexyCard.text = language[index].SexyCard;
        MachoCard.text = language[index].MachoCard;
        GirlyCard.text = language[index].GirlyCard;
        DaringCard.text = language[index].DaringCard;
        SchoolCard.text = language[index].SchoolCard;
    }*/
}
